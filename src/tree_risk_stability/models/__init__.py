"""Nested CV runner stub — implemented in Phase 3 (phase-03-model-pipeline.md).

`make_spy_fit_context` and `build_inner_cv` are QA-mandated seams from the leakage
RED suite: they expose the isolation properties (train-only inner search, inner
minority strata >= 2) as testable units instead of burying them inside the loop.
"""
